/*@import 'nib'*/
@import "fonts.css"
@import vars
@import experiments
@import blog

@import header
@import container
@import footer
@import sidebar

html, body{
	margin:0;
	padding:0;
	height:100%;
	font-family: main-font;
	background-color:white;

	min-height: 100%;
	position: relative;
}

a {
    color: black;
    text-decoration: none;
    font-size: 1.5em;
    transition:0.5s;

    &:hover{
      color: color-alizarin;
      transition:0.5s;
    }
}
